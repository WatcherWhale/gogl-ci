apiVersion: test.gogl.ci/v1alpha1
kind: TestPlan
metadata:
  name: example
  labels:
    example: "true"
spec:
  pipeline:
    branch: "master"
    variables:
      CI_COMMIT_TARGET_BRANCH: ""
  tests:
